# LESSONS

追記専用。各エントリは「却下した案 / 決め手 / 覆す条件」。

## 2026-09-22 ZIP の取得方法

- 却下した案: ダウンロードページをブラウザ操作で取得する、または非公開 API を叩く
- 決め手: ダウンロードページは SPA で静的 HTML にリンクがなかったが、JS バンドルのルーティング定義から配布 URL が `files/{年度}/rs/{ファイル名}.zip` と特定できた。ファイル単位の直接取得なら 1 回ずつで済む
- 覆す条件: 配布 URL の形式が変わる、または公式に API が案内される

## 2026-09-22 文字コード変換ライブラリ

- 却下した案: Shift_JIS を想定して golang.org/x/text を最初から入れる
- 決め手: 全 15 ファイルが UTF-8（BOM 付き）だった
- 覆す条件: 過去年度や他シート種別が別の文字コードで配布される

## 2026-09-22 `2-1` の合計行と会計別行の持ち方

- 却下した案: (a) 会計別行から再集計して合計を作る、(b) 合計行だけ保持する
- 決め手: 実データで合計行と会計別行の合計は全 18,522 グループで一致したが、公表値と導出値を混ぜると不一致時に原因を追えない。(b) は要望額（会計別行にしかない）と整合チェックの材料を失う。両方保持し、表示は合計行を基準、不一致は Issues に記録して合計は書き換えない
- 覆す条件: 合計行が配布から消える、または不一致が常態化する

## 2026-09-22 「執行が確定した最新の予算年度 N」の決め方

- 却下した案: 事業ごとに「執行額が非ゼロの最大予算年度」を N にする
- 決め手: FY2023 の合計行に執行 0 円が 483 件あり、0 円という有効な実績を欠測と取り違える。N はデータセット共通で「事業年度 − 1」とし、非ゼロ執行の最大年度は整合チェック（Notes）に使う
- 覆す条件: 配布版によって執行実績の対象年度が「事業年度 − 1」でなくなる

## 2026-09-22 大きい CSV の読み方

- 却下した案: ID → バイトオフセットの事前索引、または中間 JSON 化
- 決め手: Phase 1 は 1 事業の表示だけで、全件逐次読み取り＋ID 抽出でも数秒以内。索引や中間形式は更新時の失効判定と形式管理が増える
- 覆す条件: Phase 2 で全事業を横断するとき、または 1 回の表示が数秒を超えるとき

## 2026-09-22 全事業の読み方（Phase 2）

- 却下した案: (a) 6 ファイルを順に読んで全事業を map に保持する、(b) 中間 JSON や SQLite に落とす、(c) 非連続 ID を検出したら map にフォールバックする
- 決め手: 6 ファイルとも予算事業ID が連続・昇順だった（違反 0 件）ので、6-way マージで 1 事業ずつ流せばメモリは 1 事業分で済み、実データ 5,664 件で約 2 秒。(c) は全件メモリ実装を併設することになり、通らないコードを抱える
- 覆す条件: 配布データの並びが変わって `ErrUnordered` が出る

## 2026-09-22 一覧データの配置と JSON 埋め込み

- 却下した案: `data/summary.json` を `fetch()` で読む、`text/template` や文字列連結で index.html を組む
- 決め手: Chrome は file:// で `fetch` と ES module を拒否する。`<script type="application/json">` に埋め込めば 1 ファイルで完結し、`encoding/json` の既定エスケープが `</script>` 混入を防ぎ、`template.JS` で html/template の再エスケープを避けられる
- 覆す条件: ホスティング先が決まり file:// を切り捨てる、または JSON が 3MB を超える

## 2026-09-22 フロントのツールチェーン

- 却下した案: tsc 単体で ES modules を出す、vitest、`go build` 時に npm を呼ぶ、Preact/Lit、Chart.js
- 決め手: file:// では module script が動かないので esbuild で IIFE 1 本にする。Node 24 は .ts を直接実行できるので `node --test` で足りる。`go:embed` は存在するファイルが前提なので成果物をコミットし、`go install` に Node を要求しない。3 画面・状態はフィルタとソートだけで、バーとヒストグラムは inline SVG で足りる
- 覆す条件: Phase 3 で年度比較 UI が増えて状態管理が辛くなる（Preact 検討）、グラフの種類が増える

## 2026-09-22 断絶判定の場所

- 却下した案: フロントで閾値をスライダーで動かして再判定する
- 決め手: 判定式を Go と TS の 2 か所に持つと乖離する。閾値は `zailoop build` の引数にし、フロントは判定済みのビットを表示するだけ
- 覆す条件: 対話的に閾値を変えたい要望が強い（そのときは判定式自体を meta に載せる）
