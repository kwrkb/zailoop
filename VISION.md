# zailoop — VISION

## 目的

国家予算の「要求 → 成立 → 執行 → 決算 → 評価 → 次年度要求」というループを、**予算事業 1 件の単位で一気通貫に**可視化する。予算書・決算書・レビューシートに分断されている情報を、事業を軸に 1 画面へ束ねる。

## データ源

- 行政事業レビュー見える化サイト（内閣官房行政改革推進本部事務局）が年度ごとに配布するレビューシート CSV（15 ファイル）。
- 利用規約は公共データ利用規約（PDL1.0）準拠。本プロジェクトの出力には常に「行政事業レビュー見える化サイトのデータを加工して作成」の旨を表記する。
- サイトへのアクセスは配布 ZIP を 1 ファイル 1 回取得するだけに留める。スクレイピング・API 利用はしない。

## スコープ

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 1 | 単年度 CSV を読み、任意の 1 事業のライフサイクルを 1 画面で表示する（CLI） | 完了 |
| Phase 2 | 同じ構造体から静的 HTML を生成し、全事業を横断できるダッシュボードにする | 完了 |
| Phase 3a | 2024・2025 年度を結合し、評価・反映 → 翌年の成立・執行の「ループ検証」を行う | 完了 |
| Phase 3b | 2026 年度公開後に 3 年以上の追跡と、ID が変わった継続事業の候補提示 | 未着手 |

## 非スコープ

- 支出先の名寄せ・法人番号照合
- 財務省予算書 DB・会計検査院 DB など外部データとの結合
- サーバサイド処理、定期実行、自動デプロイ（生成した `site/` を静的ホスティングに手動で置くのは可。設計判断 13）
- 「RSシステム」「行政事業レビュー」を製品名のように使うこと（出典としての記載は可）

## 現在有効な設計判断

1. **言語は Go、stdlib のみ。** CSV は UTF-8（BOM 付き）なので文字コード変換は不要。外部依存を足すときは理由を VISION に書く。
2. **事業の一意キーは `予算事業ID`。** 年度をまたいでも同一事業を指す永続 ID であることを 2024/2025 年度で確認済み。複数年度では `(事業年度, 予算事業ID)` を複合キーにする。
3. **ライフサイクルの各段階は 1 枚のシート内の列から組み立てる。** 要求・成立・執行・決算は `2-1`、評価・翌年度反映は `4-1`、成果は `3-1`。列の対応は `docs/data-survey.md` §4 を正とする。
4. **「執行が確定した最新の予算年度 N」を軸に表示する。** N はデータから決め、N+1（事業年度）の要求額は「次年度要求」として扱う。
5. **表示は CLI テキストを先に作り、同じ構造体から HTML を生成する。** Web サーバは持たない。
6. **取得した CSV・ZIP はコミットしない。** `data/` は `.gitignore` 済み。生成した `site/` もコミットしない。
7. **全事業の読み取りは 6 ファイルを 1 回ずつ流すマージ読み。** 予算事業ID が各ファイルで連続・昇順であることを前提にし、崩れたらエラーで止める（中間形式や索引は持たない）。
8. **事業詳細は JS なしの静的 HTML、一覧・横断ビューは TypeScript。** Go 側は stdlib のみのまま。フロントの開発依存は typescript と esbuild だけで、ビルド成果物 `internal/site/assets/app.js` はコミットする（`go build` に Node を要求しない）。
9. **「ループの断絶」の判定は生成時に Go で行い、閾値は `zailoop build` の引数で変える。** フロントでは再判定しない（判定式を 2 か所に持たない）。
10. **file:// でも開ける。** 一覧データは index.html に JSON として埋め込み、リンクはすべて相対パス。
11. **複数年度は予算事業ID で結合し、重なる予算年度は新しいシートを正とする。** 古いシートとの差異は注記に残す。ID が変わった継続事業の推定は 3b の課題で、自動結合はしない。
12. **ループ検証はシート S の「反映状況」と S+1 シートの FY S+1 当初予算の増減で判定する。** 縮減・廃止・終了予定なのに増額なら「矛盾」。現状通り等は判定対象外。
13. **公開は Cloudflare Workers の静的アセット配信（`zailoop.kwrkb.workers.dev`）。** Worker スクリプトは書かず、`wrangler.jsonc` で `site/` を置くだけ（`html_handling: none`、`/` は `site/_redirects` で `index.html` にリライト）。ローカルで `zailoop build` してから `npx wrangler deploy` する手動運用で、CI からの fetch はしない（配布サイトへのアクセスを増やさない）。`site/` はコミットしない方針のまま。

## リポジトリ

`github.com/kwrkb/zailoop`（公開予定）
